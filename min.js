(function min(x,y) {
     
   if( x > y ){
            
             console.log(`${y} is minmum number `);

   }
   else{

            console.log(`${y} is maximum  number `);
   }
  
      

})(10,7);